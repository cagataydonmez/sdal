SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=YOUR_SMTP_LOGIN
SMTP_PASS=YOUR_SMTP_PASSWORD_OR_KEY
SMTP_FROM=Your Site <no-reply@yourdomain.com>
# Resend HTTP API (preferred)
# RESEND_API_KEY=YOUR_RESEND_API_KEY
# RESEND_FROM=Your Site <no-reply@yourdomain.com>
# Optional
# SMTP_SECURE=true
# SMTP_TLS_REJECT_UNAUTHORIZED=false
# Mailtrap example:
# SMTP_HOST=sandbox.smtp.mailtrap.io
# SMTP_PORT=2525
# SMTP_USER=YOUR_MAILTRAP_USER
# SMTP_PASS=YOUR_MAILTRAP_PASS
SDAL_BASE_URL=http://localhost:5173

# --- Database persistence (important in production/deploy) ---
# Preferred: set to a mounted persistent volume path (Railway Volume etc.)
# SDAL_DB_PATH=/data/sdal.sqlite
# Alternative: set a directory and db will be created as <dir>/sdal.sqlite
# SDAL_DB_DIR=/data
# Persist uploaded files (posts/photos/stories/group media) on the same volume:
# SDAL_UPLOADS_DIR=/data/uploads
#
# If you want startup to fail when DB file does not already exist:
# SDAL_DB_REQUIRE_EXISTING=true
#
# Optional one-time bootstrap copy (used only if target DB does not exist):
# SDAL_DB_BOOTSTRAP_PATH=../db/sdal.sqlite
