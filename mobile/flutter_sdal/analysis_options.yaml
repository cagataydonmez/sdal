# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

# The following line activates a set of recommended lints for Flutter apps,
# packages, and plugins designed to encourage good coding practices.
include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # Freezed/json_serializable generated annotations trigger this in generated
    # code paths; keep it ignored project-wide instead of scattering file ignores.
    invalid_annotation_target: ignore

linter:
  # Keep the lint surface intentionally close to stock `flutter_lints`.
  #
  # The audit items around token usage, localization coverage, and semantics on
  # icon-only controls are architectural conventions rather than analyzer-safe
  # rules with low false-positive rates. They remain enforced via code review
  # and targeted audit passes instead of aggressive global lints.
  rules:
    # No additional repo-wide lint overrides yet.

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options
