{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "npm --prefix sdal-modern install --include=dev && npm --prefix sdal-modern/client install --include=dev && npm run build"
  },
  "deploy": {
    "startCommand": "npm run start"
  }
}
