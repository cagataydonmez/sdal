{
  "name": "sdal-root",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "npm --prefix frontend-classic run build && npm --prefix frontend-modern run build",
    "start": "npm --prefix server run start"
  },
  "engines": {
    "node": ">=20"
  }
}
